server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Serve the frontend
    location / {
        try_files $uri $uri/ =404;
    }

    # Proxy API requests to the backend
    location /auth/ {
        proxy_pass http://backend:8787/auth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /ships {
        proxy_pass http://backend:8787/ships;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Proxy FastAPI docs
    location /docs {
        proxy_pass http://backend:8787/docs;
        proxy_set_header Host $host;
    }

    location /openapi.json {
        proxy_pass http://backend:8787/openapi.json;
        proxy_set_header Host $host;
    }
}
